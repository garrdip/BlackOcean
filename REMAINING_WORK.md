# 검은 바다 — 남은 업무 정리

> 작성일: 2026-08-04
> 기준: `GAME_DESIGN.md`(역기획서) §14 + 코드베이스 실측(master `91709304`) + `TODOLIST.md` / `NEXT_SESSION.md` / `REFACTORING_PLAN.md` 진행 상황 대조.
> 이미 처리된 항목은 제외했고, **남아 있는 것만** 정리했다. 근거는 `파일:라인` 표기.

---

## 0. 현재 상태 요약

| 트랙 | 상태 |
|---|---|
| 리팩토링 (P0~P2) | **거의 완결.** 잔여 1건 = IL2CPP 빌드 검증 |
| 카드 감사 (CARD_AUDIT A~F절) | **완결** (34회차) |
| 버그·정합성 (TODOLIST #1~#20) | **14건 완료 / 6건 잔여** — 잔여는 전부 「기획 확정 선행」 항목 |
| 3D 구체 맵 리뉴얼 | **진행 중** — 씬 기본값이 이미 3D(`GameScene.unity:8581 startWith3D: 1`), 2D 맵은 레거시로 공존 |
| 게임 완성도(§14-A 시스템 공백) | **10건 전부 미착수** — 남은 업무의 본체 |
| 컨텐츠 (카드·몬스터 추가) | 재개 가능 상태, 다만 대량 추가 전 선행 인프라 2건 권장 |

**한 줄 요약**: 코드 부채와 카드 버그는 정리가 끝났고, 지금 남은 것은 ①3D 맵 정식 전환 마무리 ②기획 확정 6건 ③"게임이 끝나지 않는" 시스템 공백(엔딩·스테이지·아이템·메타) ④출시 준비다.

---

## 1. 🔴 최우선 — 진행 중 작업 마무리

### 1-1. 3D 구체 맵 정식 전환 (가장 큰 미완결 작업)

3D 맵(`Assets/Script/Map3D/`, 2,397줄)이 이미 씬 기본 화면이지만, **여전히 "테스트" 구조**로 2D 맵과 병존한다.

- [ ] **2D/3D 로직 이중화 해소** — 방 타입 확률, 보스 존, 거점 생성, 위험도 규칙이 두 벌로 존재
  - 방 타입: `M_MapManager.cs:913-930` ↔ `SphereMapSystem.cs:288-300` (동일 확률표 복붙)
  - **위험도 규칙이 이미 갈라짐**: 3D는 「거리 1칸당 +3 + 경과 턴당 +1」(`SphereMapSystem.cs:258-272, 434-437`), 2D는 여전히 「단순 헥사 거리」(`M_MapManager.cs:362, 404`) → 2D로 되돌리면 밸런스가 달라진다
  - 조치안: 3D를 정식으로 확정하고 2D 맵 코드·씬 오브젝트를 제거하거나, 규칙을 공용 클래스로 추출
- [ ] **테스트 UI 제거** — `Map3DToggle.cs:44-53`의 `OnGUI` "3D 맵 보기 (테스트)" 버튼은 출시 빌드에 남으면 안 됨
- [ ] **세이브/로드가 3D 맵을 지원하지 않음** — `SaveData.cs`의 `SaveDataMap`은 2D 헥사 좌표·`HexagonMapRoom` 기반(`SaveDataMapRoom.coordinate/position`). 3D는 시드 기반이라 **시드·방문 타일·현재 타일·보스 타일**을 저장해야 복원 가능
- [ ] **전투 진입이 프록시 방에 의존** — `SphereMapNetwork`가 화면 밖 `HexagonMapRoom` 프록시에 방 타입/위험도를 실어 기존 파이프라인을 재사용(`SphereMapNetwork.cs:10-13`). 2D 제거 시 이 결합을 정리해야 함
- [ ] 3D 맵 기준 **플레이 테스트 전면 재검증** (아래 1-2와 함께)

### 1-2. 밀린 플레이 테스트 (코드는 커밋됐지만 검증 미완)

`TODOLIST.md` 회차 2~4의 체크리스트가 미소진 상태다. 3D 맵 전환 이후 맵 관련 항목은 특히 재확인이 필요하다.

- [ ] 회차 2: 홍단향 H5 반복 → 최대 이치 6 고정 / Twins 전투에서 Saddy 3패턴 모두 등장 / Guardian 상시 버프 등록
- [ ] 회차 3: 에리스 축복 카드·H3·H4 카드풀 등장 / 별무리 특성 툴팁 / 용어 툴팁 팝업
- [ ] 회차 4: `save.json` 저장·로드 왕복 / 에리스 HA 미생성 / 찰나 카드 총괄 덱 유지
- [ ] 회차 4 **컴파일 검증 자체가 미완** (Unity 에디터 미실행 상태에서 커밋됨) — `assets-refresh`로 확인 필요

### 1-3. 작업 트리 정리

- [ ] `Assets/_Recovery/0.unity` — 미추적 복구 씬. 필요 여부 판단 후 커밋 or 삭제
- [ ] `DebugSettings.USE_DEBUG_PLAYER_HP = true`(체력 999)가 **커밋된 상태** (`Assets/Script/Common/DebugSettings.cs:6`) — 배포 빌드 전 false 필수. 빌드 스크립트에서 강제 검사하면 안전

---

## 2. 🟠 기획 확정 대기 (결정이 있어야 코드가 진행됨)

`TODOLIST.md` 잔여 6건. **결정만 내려주면 각 1회차 이내로 처리 가능**한 것들이다.

| # | 항목 | 결정해야 할 것 | 위치 |
|---|---|---|---|
| 7 | 에리스 「꿈을 본 인형」 | DB 스펙(전투당 1회 / 현재체력 2배 이하 공격 / 체력 2 이상 복귀 시 해제) 3항목을 구현할지, **현재 코드 동작을 스펙으로 삼고 BuffDB 설명을 고칠지**. 미사용 `ERIS_NORMAL/2ND/3RD` 버프 처리도 함께 | `TargetObject.Damage.cs:48-53`, `BuffDB.csv:31,34` |
| 8 | 손패 10장 상한 | 상한을 강제할지, 강제 시 초과분을 드로우 스킵 vs 버린덱행 | `maxCardOnHandCount`(레이아웃 상수만 존재) |
| 9 | 상점 경제 | 카드 가격(현재 `SHOP_CARD_PRICE=1` **임시값**), 강화·제거 비용(현재 **무료**) | `BalanceDB.csv`, 상점 NPC |
| 13 | G2(고행 III) 사후효과 | 「고행길」이 G0=방어 -10, G1=붕괴 1인데 G2만 주석 처리로 공백 — 의도적 무효과인지 효과 부여인지 | `CardData_Geork.cs` |
| 16 | BuffDB 설명 2건 | `ERIS_2ND`/`ERIS_3RD` — #7 결정에 종속 | `BuffDB.csv` |
| 18 | 로비 규칙 | 비밀번호 실검증 도입 여부 / 캐릭터 중복 선택 허용 여부 | `M_SteamManager.cs:134-135`, `CharacterSelectUI.cs:29-37` |

> #10(행동비용 초기값 3 vs 최대 30)은 커밋 `95461b1d`·`91709304`에서 **해소됨** — 시작값 = `maxActionCost`(30), 이동은 거리 무관 1턴, 미클리어 방 정리에서 1턴 추가.

**추가 확정 대기(문구 차원)**: "찬란한 고행 I/II/III"(CardDB.csv 128~137행, G0_H~G2_H) 표시 이름, "광기" 용어(BuffDB.csv 34행) — `NEXT_SESSION.md` 기록.

---

## 3. 🟡 시스템 공백 — "게임을 게임으로 완성"하는 작업 (§14-A, 10건 전부 미착수)

현재 상태로는 **런이 끝나지 않는다.** 아래가 남은 업무의 본체다.

### 3-1. 런 종결 구조 (최우선)

- [ ] **게임 클리어/엔딩 없음** — 맵 보스를 잡아도 일반 전투처럼 보상 후 맵으로 복귀. 클리어 판정·엔딩 연출·결과 화면 전무 (`GameOver` 경로만 존재: `GamePlayer.cs:123`, `PopUpUIManager.cs:437`)
- [ ] **스테이지 1→2→3 진행 없음** — 스크립트/프리팹은 준비 완료(S2 12종, S3 11종+엘리트 2종, 커밋 `662345b1`에서 프리팹화·네트워크 등록)이나 **MonsterDB.csv 미등록 → 스폰 불가**. 남은 일은 ①행동 패턴 데이터 작성 ②스테이지 전환 규칙 설계
- [ ] **난이도 선택 없음** — `GameLevel{EASY,NORMAL,HARD}`가 `ProjectD.cs:12`에 선언만 되고 참조 0건

### 3-2. 방·보상 컨텐츠

- [ ] **이벤트 방 실효과 없음** — 긍정/부정 모두 토스트·BGM·보이스 연출만 (`M_TurnManager.Presentation.cs:166,180`). 이벤트 테이블 설계 + 선택지 UI 필요
- [ ] **아이템 상점(ITEM_NPC) 구매 로직 없음** — 섀도우맨은 스폰되지만(`BattleSpawner.cs:81`) 구매 경로 미구현
- [ ] **보상 테이블 하드코딩** — 골드 10 + 카드 3택1 고정, DB 조회 TODO 주석 상태 (`RewardService.cs:27`, `RewardListItem.cs:62,68`)
- [ ] **엘리트 전용 몬스터 테이블 없음** — ELITE 방이 일반과 동일한 hazard 그룹을 스폰, 연출만 차이 (`M_TurnManager.Spawner.cs:18`)
- [ ] **거점(Region) 등급 효과 없음** — LEGEND/UNIQUE/RARE/NORMAL이 툴팁 색상만 좌우

### 3-3. 아이템·메타 진행

- [ ] **아이템(유물)/유산 시스템 스텁** — `ArtifactDB.csv`/`LegacyDB.csv`에 `TEST`·`TEST2` 2행뿐, 효과 메서드 빈 함수, 획득 경로 0건. 프레임워크(등급 4종·트리거 12종)만 존재
- [ ] **메타 프로그레션 전무** — 런 간 영구 성장·해금·통계 없음. "유산(LEGACY)"은 이름만
- [ ] **캐릭터 시작 덱 구성 코드 전체 주석 처리** — 신규 시작 시 **덱이 비어 있음** (`GamePlayerDeck.cs:202-249`). 3인 시작 덱 구성 확정 + 코드 복원 필요 (게임을 처음부터 플레이하려면 사실상 필수)

---

## 4. 🟢 컨텐츠 대량 추가 전 선행 인프라 (권장)

`NEXT_SESSION.md` 기준, 카드·몬스터를 대량으로 늘리기 **전에** 하면 이득인 설계 작업.

- [ ] **P1-8b — 카드 실행 골격의 데이터 주도 실행기**: 현재 카드 1장 = 코루틴 1개(디밍→애니→대기→효과)를 손으로 작성. 카드 199장 → 확장 시 비용이 선형 증가. 실행기 도입 시 CSV/SO 데이터로 대체 가능
- [ ] **몬스터 `DoAction` 데이터화(ScriptableObject)**: 스테이지 2·3 몬스터 23종을 등록하기 전에 하면 복붙 20개 패턴이 반복되지 않음
- [ ] **TargetObject 플레이어/몬스터 완전 분리**: 4번째 캐릭터 추가 등 필요 시점에

---

## 5. 🔵 출시 준비 (Steam 배포)

- [ ] **P2-17 IL2CPP 빌드 검증** — 리팩토링 계획의 유일한 잔여 항목. Unity Hub에서 Windows Build Support(IL2CPP) 설치 → 백엔드 전환 → 빌드 → Steam 켠 상태로 Steamworks.NET 동작 확인 (Linux Mono 스모크 빌드는 20회차에서 성공)
- [ ] **자동 저장 없음** — 세이브 트리거가 테스트 버튼(`Assets/Script/TEST/SaveTest.cs`)뿐, 로드는 로비명 `"load"` 히든 트리거(`CreateLobby.cs:24-28`). 정식 저장/이어하기 UX 필요 (3D 맵 저장 대응은 1-1 참조)
- [ ] **접속 해제·재접속 처리** — `OnServerDisconnect`(`M_NetworkRoomManager.cs:86`)가 기본 처리만. 게임 중 이탈 시 파티 진행(전원 합의 지점 4곳)이 막히지 않는지 검증 필요
- [x] ~~**다국어 시스템 기반 구축**~~ — ✅ 완료 (2026-08-04). 키 기반 스트링 테이블 방식으로 재구축. 상세: `LOCALIZATION.md`
  - `Assets/Resources/Language/`(Locales.csv + 언어별 CSV), 언어 추가 = **CSV 1장 + Locales.csv 한 줄**
  - 한국어 원문은 DB CSV에 그대로 유지(폴백) → 카드 추가 시 번역 파일을 함께 고칠 필요 없음
  - 카드 설명 마크업을 중괄호 명시형(`@{용어}` `!{15}` `${4}{3}`)으로 전환 — CardDB 398행 718토큰 변환 완료, 공백 없는 중국어·일본어에서도 파싱됨
  - 조사(을/를) 자동 부착은 한국어일 때만 적용, 스팀 클라이언트 언어 자동 감지, 옵션 드롭다운 실제 동작
  - 에디터 도구(**Tools ▸ 현지화**): 번역 스켈레톤 내보내기 / 누락 리포트 / 마크업 검증
  - 멀티 언어 혼재 대응: Mirror가 `CardBase`를 통째로 동기화하므로 카드 표시는 `CardData.GetLocalizedName/Description`을 거치도록 수정
- [x] ~~**영어 지원 1차 완료**~~ — ✅ (2026-08-04)
  - 번역 키 **1,079개 전량 영어 작성** (카드 796 · 버프 86 · 용어 78 · 특성 17 · UI 102). 미작성 2건은 한국어 원문도 없는 `ERIS_2ND/3RD` 설명
  - 하드코딩 UI 텍스트 **62개**에 `TextUpdater` + 키 부여 (GameScene 39 · MenuScene 6 · 프리팹 17). 런타임에 값이 바뀌는 텍스트 11개는 의도적으로 제외하고 코드 쪽 키로 처리
  - 코드 내 사용자 노출 문자열 **32곳**을 `M_LanguageManager.Get(키, 원문)`으로 이관 (토스트·전열/중열/후열·위험도 표시·팝업 안내문 등)
  - **한글 깨짐 수정**: `TMP Settings`의 전역 폴백이 비어 있고 메인메뉴가 라틴 전용 폰트(LiberationSans, 글리프 250자)를 쓰고 있었음 → `NotoSansKR-Bold SDF`(11,522자)를 전역 폴백에 등록
- [ ] **다국어 — 남은 작업**
  - **영어 원어민 검수** — 현재는 1차 번역. 고유명사는 `LOCALIZATION.md` 용어집 기준이며, 용어 변경은 CSV 한 칸 수정으로 전 카드에 반영된다
  - **추가 언어**(일·중·프·스): 에디터 도구로 스켈레톤 내보내기 → 번역 → `Locales.csv` 한 줄 추가
  - **폰트**: CJK 추가 시 TMP 폰트 에셋 사전 생성 + fallback 체인 구성, 재배포 가능 라이선스 확인 (일본어 가나·중국어 간체는 현재 폴백 폰트에 없음)
  - **UI 레이아웃**: 프랑스어·스페인어는 한국어 대비 30~50% 길어짐 → 고정폭 버튼 오버플로 대응(auto-size/줄바꿈)
  - 3D 맵 테스트 UI(`Map3DToggle`의 OnGUI 버튼)와 남은 디버그 문구는 정식 전환 시 제거 대상 — 번역 대상에서 제외했다
- [ ] **관전 모드 미구현** — 사망 플레이어가 할 일이 없음 (`CardPocket.cs:10` TODO). 3인 협동에서 1인 조기 사망 시 체감이 나쁨

---

## 6. ⚪ 잔여 잡항목 (소규모)

- [ ] **SUHOJA 버프 미완** — Guardian 상시 버프인데 ①`BuffData.buffIcons`(MenuScene)에 아이콘 미등록 ②효과 코드 미구현 ③BuffDB.csv 행 없음 (TODOLIST #12 잔여)
- [ ] **Saddy 상시 버프 `E2` 정체 불명** — 존재하지 않는 BuffType. 현재 로드 시 해당 행 스킵 + 에러 로그 1건(의도된 동작). 어떤 버프인지 기획 확정 필요
- [ ] **MonsterDB `치유사` 엔트리** — 대응 몬스터 스크립트 없음(등록 22엔트리 중 유일). 미사용인지 확인
- [ ] **엘리트 `E3`/`E4` 명명** — 실제 클래스는 `Assassin`/`Executioner`. DB 이름과 클래스명 정합성 정리
- [ ] **보스 대사 분기 TODO 3건** — "에리스 공허(광기) 상태인 경우 대화 분기" (`Boss_Momos.cs:37`, `Boss_Apates.cs:47`, `Boss_Geras.cs:37`)
- [ ] **`TextDetector` 용어 팝업 경로 미구현 4건** — 현재 툴팁은 `CardData.ResolveInfoKey` 경로로 동작하므로, 이 클래스가 죽은 코드인지 확인 후 제거 or 완성 (`TextDetector.cs:74,76,98,100`)
- [ ] **`CardData_Eris.cs:853` TODO** — "파괴의 권능 효과를 N+1배 더 받습니다" 미반영 카드 1건
- [ ] **플레이어 도트뎀 표시 미구현** (`NamePlate.cs:72`)
- [ ] **`M_MapManager.CheckTargetObject()` 0.1초 폴링 루프** (`M_MapManager.cs:806-815`, TODO 주석) — 이벤트/콜백 전환 여지

---

## 7. 권장 진행 순서

```
1단계  3D 맵 정식화 결정 → 2D 제거 or 규칙 공용화 → 세이브 3D 대응 → 테스트 버튼 제거
       (동시에 밀린 플레이 테스트 소진, 회차 4 컴파일 검증)
   ↓
2단계  기획 확정 6건 회신 → 각 1회차씩 소화 (#7·#8·#9·#13·#16·#18)
   ↓
3단계  【런 완성】 시작 덱 복원 → 게임 클리어/엔딩 → 보상 테이블 DB화
       ※ 여기까지 하면 "시작~끝"이 성립하는 빌드가 나옴
   ↓
4단계  선행 인프라(P1-8b, 몬스터 데이터화) → 스테이지 2·3 등록 → 엘리트 테이블 → 이벤트 방
   ↓
5단계  아이템/유산 본구현 → 메타 프로그레션 → 난이도 선택 → 거점 등급 효과
   ↓
6단계  출시 준비: IL2CPP 빌드 · 자동 저장 · 접속 해제 처리 · 디버그 플래그 정리
```

**가장 빨리 체감이 바뀌는 지점은 3단계**다 — 시작 덱과 엔딩만 들어가도 "플레이 가능한 한 판"이 완성된다. 반대로 3D 맵 이중화를 방치한 채 컨텐츠를 얹으면 두 벌 규칙을 계속 따라 고쳐야 하므로 1단계를 먼저 닫는 것이 낫다.

---

## 관련 문서

| 문서 | 역할 |
|---|---|
| `GAME_DESIGN.md` | 역기획서 — 시스템 전수 명세, §14가 본 문서의 원본 목록 |
| `TODOLIST.md` | 버그·정합성 20건 트래커 (회차별 진행 로그) |
| `NEXT_SESSION.md` | 리팩토링·카드 감사 회차 이력, 다음 세션 체크리스트 |
| `REFACTORING_PLAN.md` / `REFACTORING_RESULT.md` | 리팩토링 계획·완료 내역 |
| `CARD_AUDIT.md` | 카드 감사(완결) |
