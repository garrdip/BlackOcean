# 검은 바다 (BlackOcean) — 게임 기획 문서 (역기획서)

> 코드베이스 전수 분석(2026-07-29 기준)을 바탕으로 작성한 역기획 문서.
> 근거는 `파일:라인` 형태로 표기. 수치는 `Assets/Resources/DB/BalanceDB.csv` 및 각 CSV DB 실측값.

---

## 목차

1. [게임 개요](#1-게임-개요)
2. [게임 흐름 (씬·로비·로딩)](#2-게임-흐름)
3. [맵·진행 시스템](#3-맵진행-시스템)
4. [전투 시스템 (턴 상태머신)](#4-전투-시스템)
5. [카드 시스템](#5-카드-시스템)
6. [자원·밸런스 수치](#6-자원밸런스-수치)
7. [캐릭터별 고유 능력 상세](#7-캐릭터별-고유-능력-상세)
8. [버프/디버프 전체 목록](#8-버프디버프-전체-목록)
9. [몬스터·적 구성](#9-몬스터적-구성)
10. [3인 협동 규칙](#10-3인-협동-규칙)
11. [보상·상점·휴식·아이템](#11-보상상점휴식아이템)
12. [저장·메타 진행](#12-저장메타-진행)
13. [승리·패배 조건](#13-승리패배-조건)
14. [미구현·기획 확인 필요 목록](#14-미구현기획-확인-필요-목록)

---

## 1. 게임 개요

| 항목 | 내용 |
|---|---|
| 장르 | **최대 3인 협동 멀티플레이 덱빌딩 로그라이크 카드 게임** |
| 플랫폼 | Steam (앱 ID 2359700) |
| 엔진 | Unity 2021.3.18f1 |
| 네트워크 | Mirror(호스트 서버 권위) + Steamworks(로비 매치메이킹) |
| 플레이어 캐릭터 | 게오르크(GEORK) / 에리스(ERIS) / 홍단향(HONGDANHYANG) |
| 핵심 루프 | 헥사곤 오픈 맵 탐험 → 방(전투/상점/캠프/이벤트) 공략 → 카드 획득·강화 → 행동비용 소진 시 맵 보스 출현 → 보스전 |

**핵심 설계 특징**
- 슬레이 더 스파이어식 노드 트리가 아닌 **육각형(axial 좌표) 오픈 필드 맵** + **행동비용(턴) 자원** + **보스 추격**이라는 독자 구조.
- 전투는 턴제지만 **플레이어 3인이 동시에 행동**하는 실시간 협동 턴 방식 (순번제 아님).
- 모든 게임 데이터(카드/버프/몬스터/밸런스)는 CSV 기반, 카드 효과는 **리플렉션으로 메서드 바인딩**.
- 서버(호스트) 권위: 턴 전이·카드 실행·버프·데미지는 전부 호스트에서 실행, 클라는 SyncVar/SyncList/RPC 수신.

---

## 2. 게임 흐름

### 2-1. 씬 흐름

```
MenuScene ──싱글(즉시 StartHost)/멀티(Steam 로비)──▶ RoomScene ──전원 Ready──▶ GameScene
```

### 2-2. MenuScene

- **싱글플레이**: `maxConnections=1`로 낮추고 Steam 로비 없이 즉시 `StartHost()` (`MenuUI.cs:32-39`)
- **멀티플레이**: Steam 로비 생성/목록 (`SteamMatchmaking.CreateLobby(Public, 3)` — 최대 3인, `M_SteamManager.cs:42-47`)
- **덱북(카드 도감)**: 캐릭터별 카드 열람 — 현재 유일한 메뉴 메타 콘텐츠 (`DeckBookTab.cs`)
- Steam 초기화 실패 시 `SteamFailUI` 표시.

로비 데이터 키 3종: `HostAddress`(호스트 SteamID), `LobbyName`, `Password`. 참가자는 `HostAddress`를 `networkAddress`에 세팅 후 `StartClient()` (`M_SteamManager.cs:80-96`).
- ⚠ 비밀번호는 **표시만 되고 검증되지 않음** (자물쇠 아이콘만, `M_SteamManager.cs:134-135`).
- 🔑 히든 기능: 로비 이름을 정확히 `"load"`로 입력하면 세이브 로드 (`CreateLobby.cs:24-28`).

### 2-3. RoomScene (캐릭터 선택·오더 편성)

- 접속 시 서버가 **RoomPlayer + LobbyPlayer 페어** 생성, `PlayOrder`(FIRST/SECOND/THIRD)·색상(빨/초/파)·SteamID 배정 (`M_NetworkRoomManager.cs:43-83`).
- 캐릭터 선택: 클릭 → `roomPlayer.character` SyncVar 대입 (`CharacterSelectUI.cs:29-37`). ⚠ **중복 선택 방지 없음** — 3인 전원 같은 캐릭터 가능.
- 오더(슬롯) 스왑: 빈 슬롯이면 즉시 이동, 사람이 있으면 **수락/거절 요청** 방식 (`M_LobbyMananger.cs:28-51`).
- 시작 조건: 호스트 제외 전원 Ready + 전원 캐릭터 선택 완료 → 호스트 버튼이 "START"로 변경 → `ServerChangeScene` (`M_LobbyMananger.cs:116-135`).

### 2-4. GameScene 로딩 시퀀스

`LOADING_STATE` 순서로 서버가 진행, 각 단계마다 전원 `workDone` 집계 (`M_LoadingManager.cs:47-119`):

```
ROOM_SCENE → SCENE_LOADING → MAP_GENERATE(맵+거점 생성)
→ GAMEPLAYER_COMPONENT_GEN(덱 생성) → UPLOAD_AVATAR(스팀 아바타)
→ MAP_SCENE(거점 색칠+로딩 해제) → LOADING_GAME_SCENE → GAME_SCENE
```

플레이어 초기 스탯: **HP 50 / 회복량 15 / 골드 100** (`PlayerInterface.cs:131-134` ← BalanceDB).

---

## 3. 맵·진행 시스템

### 3-1. 맵 구조

- 중앙 시작방 + 6방향 활성 방으로 시작, **반경 15칸**까지 비활성 방을 사전 생성 (`M_MapManager.cs:308-412`).
- **위험도(hazard) = 시작점으로부터의 헥사 거리** — 몬스터 그룹 선택 기준이자 사실상의 난이도 축 (`M_MapManager.cs:360`, `BattleSpawner.cs:47`).
- **시야(mapSight) = 1**: 이동할 때마다 현재 방 주변 1칸 활성화 (`M_MapManager.cs:452-469`).

### 3-2. 방(RoomType) 종류와 생성 확률

`GetRoomType()` (`M_MapManager.cs:913-923`):

| 방 타입 | 확률 | 진입 시 |
|---|---|---|
| MONSTER (일반 전투) | 40% | hazard 기반 몬스터 그룹 전투 |
| ELITE (엘리트) | 10% | ⚠ 실제로는 일반과 같은 hazard 그룹 스폰 — 연출(토스트/BGM)만 다름 |
| CAMP (전초기지) | 10% | NPC 류진솔/소피아 랜덤, 전원 `recoveryLimitCount=1` 부여 |
| CARD_NPC (카드 상점) | 10% | NPC 메르쿠리우스, 캐릭터별 카드 6장 롤 |
| ITEM_NPC (아이템 상점) | 10% | NPC 섀도우맨 — ⚠ 구매 로직 미구현 |
| EVENT_POSITIIVE | 10% | ⚠ 토스트+BGM+보이스 연출만, 실효과 없음 |
| EVENT_NEGATIVE | 10% | 동일 (연출만) |
| BOSS / RUINS / COMPLETE | 동적 | 보스 출현 시 변환 / 보스가 지나간 자리 / 클리어한 방 |

### 3-3. 거점 지역 (Region)

- 런 시작 시 **6~8개 지역** 생성 (`M_MapManager.cs:476`), 등급 LEGEND/UNIQUE/RARE/NORMAL 각 25% (`Region.cs:13-32`).
- 등급별 타일 수: NORMAL 4~5 / RARE 5~7 / UNIQUE 6~9 / LEGEND 7~11.
- ⚠ **등급의 게임플레이 효과는 현재 없음** (마우스오버 텍스트 색상만).

### 3-4. 행동비용과 맵 보스 추격 — 핵심 압박 장치

초기값: `actionCost=1, maxActionCost=30, currentActionCost=3` (`M_MapManager.cs:189-195`) — ⚠ 최대 30 대비 시작 3, 수치 검증 필요.

**소모**: ① 방 이동 시 A* 경로 길이만큼 ② 방 클리어 시 기본 1 (`M_MapManager.cs:651-666`, `M_TurnManager.Reward.cs:28`).

**행동비용 0 → 맵 보스 출현** (`M_MapManager.cs:661-663`):
- 맵 가장자리(거리 13~15) 랜덤 스폰 → 이후 플레이어 이동마다 **2칸씩 추격** (`:570-590`, `:669-694`)
- 보스 출현 후 플레이어는 **1칸 초과 이동 금지** (`GamePlayerMap.cs:25-28`)
- 보스 주변 반경 2칸이 BOSS 방으로 변환, 이전 보스방은 RUINS화 (`:733-752`)
- 접촉/BOSS 방 진입 시 3종 보스(모모스/아파테스/게라스) 중 랜덤 전투

### 3-5. 파티 이동 — 투표제 (전원 함께 이동)

1. 각자 맵 타일 클릭 → A* 경로 탐색(통과 가능 조건: COMPLETE/START_LOCATION 방만) → 투표 등록 (`GamePlayerMap.cs:21-56`, `M_MapManager.cs:1156-1167`)
2. 전원 Ready → 집계 (`M_MapManager.cs:284-305`):
   - 2명 이상 같은 방 → **과반수 확정**
   - 전원 다른 방 → **후보 중 완전 랜덤**
3. **파티 전원이 같은 방으로 함께 이동** (개별 이동 없음). 목적지가 미클리어 방이면 전투/이벤트 진입.
4. 맵에서도 전열/중열/후열 오더 스왑 가능 (수락/거절 방식, `MapPlayer.cs:134-212`).

---

## 4. 전투 시스템

### 4-1. 턴 상태머신 개요

`M_TurnManager`의 `[SyncVar] BattleTurn phase` — 전이 로직은 전부 `[Server] OnChangedPhase()` (`M_TurnManager.cs:183-233`). 클라에는 페이즈 이름 텍스트만 RPC 전달.

```
(전투 진입) → BATTLE_INITIALIZE → BATTLE_STANDBY ─┐
   ┌──────────────────────────────────────────────┘
   ▼
BATTLE_STANDBY   몬스터 다음 행동 예고(가중치 룰렛/시퀀스) ← 매 턴 루프 헤드
   ▼
PLAYER_PREEFFECT 방어도 0 초기화, 턴시작 버프 훅, 버프 스택 감소(개화는 무조건 제거)
   ▼
PLAYER_DRAW      이치 전액 회복(current=max), 5장 드로우(클라가 Cmd로 요청)
   ▼
PLAYER_ACTIVE    ★ 3인 동시 카드 사용 구간 (입력 대기) — 전원 턴종료 시 진행
   ▼
PLAYER_ACTIVE_DONE   카드 큐 소진 대기
   ▼
PLAYER_END_TURN_EFFECT  턴종료 버프 훅, 영웅 지속턴 -1, 철귀 자동 효과
   ▼
PLAYER_END       손패 정리(저주 사후효과 등록, 영원 카드는 유지) → 전원 완료 시
   ▼
MONSTER_ORDERSELECT → MONSTER_PREEFFECT(몬스터 방어 0, 디버프 정산) → MONSTER_ACTIVE(순차 행동)
   ▼
BATTLE_STANDBY (다음 턴) …… 몬스터 전멸 시 → BATTLE_END(보상)
```

비전투 방은 `NONE_BATTLE_SCENE`(NPC 상호작용) → 전원 턴종료 → `NONE_BATTLE_END`.
※ `PLAYER_ORDERSELECT`는 enum에만 있는 데드 상태.

### 4-2. 전투 진입 처리

1. 방 진입 → `GenerateBattleObject()` → BattleSpawner가 플레이어 유닛(슬롯 0~2)·몬스터(슬롯 3~5)·NPC 스폰
2. 전 클라 RPC로 카드 셔플: 총괄 덱(`deck`) → 뽑을덱(`prefareDeck`) 깊은복사 + 피셔-예이츠 셔플
3. 어빌리티 카드(HA) 스폰
4. 전원 초기화 완료 대기 후 `BATTLE_INITIALIZE` (홍단향에게 철귀 버프 `4+영구성장분` 부여)

### 4-3. 카드 사용 파이프라인 (3단 큐, 서버 직렬 실행)

```
[클라] 드래그(논타겟: 화면 상단 절반) / 베지어 화살표(타겟) → Cmd
   ↓
[서버] GamePlayerDeck.serverCardPredictQueue (검증 큐)
   ↓ EnQueueCardTargetPair: 코스트 검산→이치 차감, 타겟 null 반려
[서버] M_TurnManager.cardTargetPairQueue (실행 큐, 전역 FIFO — 3인 공용)
   ↓ ProcessCardQueue: 타겟 사망 재검증(반려 시 이치 환불)
   ↓ 카드사용 버프 훅 → 파괴의권능 배수 적용 → CardData.RunCard (리플렉션 코루틴)
   ↓ 도돌이표(공격 2회) / 별무리 후처리 / 화합 / 숙련·중력 비용 가감
[서버] 카드 행선지: 찰나→잊혀진덱 / 기본→버린덱 / 지치지않는자·별무리전략→패 복귀
```

TargetObject 리스트 규약: `[0]`=시전자, `[1]`=지정 타겟(논타겟이면 없음), 이후 전체 플레이어+몬스터 (`GamePlayerDeck.cs:286-292`).

### 4-4. 드로우 규칙

- 매 턴 **5장** (`DRAW_COUNT_PER_TURN`). 뽑을덱에서 매 드로우마다 랜덤 인덱스 추출.
- 뽑을덱이 비면 **버린덱 전체를 뽑을덱으로 충전** (잊혀진덱은 충전 대상 제외 = 전투 중 소멸).
- 둘 다 비면 남은 드로우 중단(방어 코드).

### 4-5. 피해 계산 순서

**플레이어 피격** (`TargetObject.Damage.cs:22-60`):
```
단말마(×2) → 붕괴(×1.5 내림) → 전체 개화 합계만큼 감소 → 방어 흡수 → HP 차감
→ HP≤0: 에리스(비광기)면 HP=1+광기 변신, 그 외 사망
→ 잃은 체력만큼 템페스토소 누적(10당 1드로우)
```

**몬스터 피격** (`TargetObject.Damage.cs:138-173`):
```
파괴의권능 배수(가장 먼저) → 붕괴(×1.5) → 전체 개화 합계만큼 증가
→ 월식이면 피해량만큼 시전자 방어 → 방어 흡수 → HP 차감 → isDying 처리
```

- 방어도는 **플레이어·몬스터 모두 매 턴 0으로 초기화** (이월 없음).
- 몬스터 사망은 2단계(즉시 `isDying`+디졸브 연출 → 카드 실행 종료 후 실제 제거)로 직렬화. 엘리트/보스 처치 시 카운터 집계(G56 전리품 수집, H60 등에 사용).

---

## 5. 카드 시스템

### 5-1. 데이터 구조

- **CardBase** (CSV 원본, 불변): 번호·캐릭터·이름·설명·타입·코스트·타겟팅·MaxExp·특성 (`CardBase.cs`)
- **Card** (인스턴스): `guid`(식별키), `isEnhanced`(영구 강화), `costAddition`(숙련/중력 가감), `experience`(사용 횟수), `tempEnhanced`(전투 한정), `stackCount`(영구 스택, G56용) 등 (`Card.cs`)

**CardDB.csv 컬럼**: `CardNo`(= 실행 메서드명), `Character`, `Name`, `Description`, `TYPE`, `Cost`, `Targetable`(Y/N), `ValidTarget`, `MaxExp`, `Characteristic`(가변 반복).

**리플렉션 바인딩**: `Delegate.CreateDelegate`로 `CardNo`와 동명의 코루틴 메서드(`CardData_Geork/Eris/DanHyang.cs`)를 바인딩. `_E`(강화) 메서드가 없으면 기본 메서드 폴백. 카드 추가 = **CSV 행 + 동명 코루틴 메서드**.

**설명문 마크업**: `@용어`(툴팁), `*카드번호`(카드 참조), `~덱이름`(굵게), `!피해`(힘의이치+개화 실시간 반영), `#방어`(방어의이치 반영), `^체력`, `&크기`, `$피해$타수`.

### 5-2. 카드 총량·분포 (실측)

- 전체 398행 = **기본 199장 + 강화(_E) 199장** (1:1 대응)
- 캐릭터별(기본): **게오르크 76 / 홍단향 62 / 에리스 61** — 공용 카드 0장
- 타입(기본 199): 전략 86 / 공격 79 / 축복 18 / 저주 8(게오르크 전용 G0~G7) / 영웅 8(저주의 변신형 G0_H~G7_H) / WOUND 0(미사용)
- 코스트 분포(기본): 0(18) / 1(75) / 2(61) / 3(31) / 4(4) / 5(3) / 6(1) / 8(2) / 15·20·25(4)
- 평균 코스트: 게오르크 1.89 / 에리스 1.95 / **홍단향 2.58**(무거운 빌드업형)
- MaxExp: 198장이 **5** (5회 사용 시 다음 사용이 강화 효과 후 리셋), HA(철귀이동)만 999

**초고코스트 4장**(H10 철의 꽃밭 25 / H13 차오르는 복수심 20 / G56 전리품 수집 20 / H50 기다림의 미학 15)은 전부 **해방(HEBANG)** 특성 — 이번 전투 사용 카드 수만큼 비용 감소 → 장기전 피니셔 설계.

### 5-3. 카드 특성 (CardCharacteristic)

| 특성 | 한글명 | 동작 |
|---|---|---|
| GOOWON | 구원 | 타인 대상 시 방어/버프 수치 **2배** (협동 특화) |
| YOUNGWON | 영원 | 턴 종료 시 버려지지 않고 손에 유지 |
| CHALNA | 찰나 | 사용 후 **잊혀진덱**행 (전투 중 소멸) |
| NON_CHALNA | 버린덱으로보내짐 | 찰나 무효화 |
| HEBANG | 해방 | 비용 -= 이번 전투 사용 카드 수 |
| JOONGREUK | 중력 | 사용할 때마다 비용 +1 (영구) |
| SOOKREON | 숙련 | 사용할 때마다 비용 -1 (영구) |
| EUNHASOO | 은하수 | 직전 카드와 같은 타입이면 비용 -1, 다르면 +1 |
| GISADO | 기사도 | 몬스터의 다음 타겟에 자신이 포함되면 효과 반복 |
| HWAHAP | 화합 | 사용 후 철귀 패시브(위치 기반 방어/피해) 즉시 1회 발동 |
| BYULMOORI | 별무리 | 별무리 버프 연동 |
| FORWARD/BACKWARD | 전진/후진 | 진영(열) 이동 |
| BOONGGUI/SOIRAK | 붕괴/쇠락 | 동명 버프 부여 계열 |
| GOHENG | 고행 | 이 특성 카드는 보상/상점 풀에서 제외 |
| GEUNWON | 근원 | 카드별 개별 구현 |

특성 부여 통계(기본 199장, 83건): 찰나 26 / 은하수 14 / 구원·중력·영원 각 7 / 숙련 5 / 화합 4 / 전진·후진·별무리 각 3 / 해방·붕괴 각 2.

### 5-4. 4개 덱 구조

| 덱 | 역할 |
|---|---|
| `deck` (총괄 덱) | 게임 전체 소유 카드. 보상/상점 획득·강화·제거 대상 |
| `prefareDeck` (뽑을 덱) | 전투용. 시작 시 총괄 덱 깊은복사+셔플. 비면 버린덱에서 충전 |
| `trashDeck` (버린 덱) | 사용/버린 카드 |
| `forgottenDeck` (잊혀진 덱) | 찰나 카드 전용. 충전 대상 제외 = 전투 중 영구 소멸 |

전투 종료 시 뽑을덱+버린덱 → 총괄 덱 회수. ⚠ 잊혀진덱은 `Clear()`만 되고 회수 경로 없음(원본은 총괄 덱에 남아 있으므로 실질 문제는 검증 필요).

### 5-5. 획득·강화·제거 경로

| 경로 | 내용 |
|---|---|
| 전투 보상 | 자기 캐릭터 풀에서 랜덤 3장 중 1택 (스킵 가능) |
| 카드 상점 (메르쿠리우스) | 캐릭터별 랜덤 6장, 가격 1골드(임시값) |
| 영구 강화 | 상점 NPC에서 `isEnhanced=true` — ⚠ 현재 골드 소모 없음 |
| 경험치 강화 | 5회 사용마다 다음 1회가 `_E` 효과 |
| 제거 | 상점 NPC에서 총괄 덱에서 영구 제거 — ⚠ 골드 소모 없음 |
| 전투 중 생성 | 카드 효과로 손패 직접 생성 (덱에 안 남음) |

보상/상점 풀 제외 규칙: `_E` 강화판, `HA`, 고행(GOHENG) 특성 카드 (`M_CardManager.cs:69-78`).

---

## 6. 자원·밸런스 수치

### 6-1. 이치(理致) — 전 캐릭터 공용 카드 코스트 자원

> "이치"는 특정 캐릭터 능력이 아니라 **마나/에너지에 해당하는 공용 자원**이다 (`GamePlayerDeck_IchiPart.cs`).

| 항목 | 값 |
|---|---|
| 초기/최대 이치 | 게오르크·홍단향 **3**, 에리스 **2** |
| 턴 시작 | `currentIchi = maxIchi` 전액 회복 |
| 상한(`ICHI_LIMIT`) | 6 — ⚠ 선언만 되고 실제 클램프 미적용 |
| 부족 시 | 카드 반려(패로 복귀), 타겟 무효 시 환불 |
| 전투 종료 | ⚠ `maxIchi=3; currentIchi=3` **하드코딩 리셋** (`RewardService.cs:53-54`) — 에리스의 2, H5/H6로 올린 최대치 무시 |

**실코스트 계산 순서** (`GamePlayerDeck.cs:253-281`):
```
기본코스트 + costAddition + (고행3 디버프면 +1)
→ 은하수(±1) → 해방(-사용카드수) → 별무리(비공격/전략 -2)
→ E57(-은하수 카드 수) → 고행3 버프면 최종 1로 고정 → 최소 0
```

### 6-2. BalanceDB.csv 전체

| 키 | 값 | 의미 |
|---|---|---|
| PLAYER_INIT_HP | 50 | 초기=최대 체력 |
| PLAYER_INIT_RECOVERY | 15 | 캠프 회복량 |
| PLAYER_INIT_GOLD | 100 | 초기 골드 |
| ICHI_INIT_DEFAULT / ICHI_INIT_ERIS / ICHI_LIMIT | 3 / 2 / 6 | 이치 |
| DRAW_COUNT_PER_TURN | 5 | 턴당 드로우 |
| BATTLE_REWARD_GOLD | 10 | 전투 승리 골드 |
| SHOP_CARD_PRICE | 1 | 상점 카드 가격 (**임시값** 명시) |
| SHOP_CARD_MAX / REWARD_CARD_MAX / REMOVE_CARD_MAX | 6 / 3 / 3 | 상점 슬롯 / 보상 선택지 / 패 제거 상한 |
| HERO_DURATION_TURN / HERO_BONUS_PER_CARD | 3 / 3 | 게오르크 영웅 변신 지속턴 / 영웅카드당 보너스 |

손패 상한: `maxCardOnHandCount=10`은 레이아웃 계산용 상수일 뿐 **실제 상한 강제 미구현**.

---

## 7. 캐릭터별 고유 능력 상세

> 3인 공통: HP 50 / 회복 15 / 골드 100. 수치 차이는 **이치(에리스만 2)** 뿐.
> ⚠ 캐릭터별 시작 덱 구성 코드는 현재 **전부 주석 처리**되어 신규 시작 시 덱이 비어 있음 (`GamePlayerDeck.cs:202-249`).

### 7-1. 게오르크 (GEORK) — 「위대한 자」 + 「영웅 변신」

**컨셉**: 저주 카드 "고행" 3종을 모아 **영웅으로 승화**하는 리스크-리워드형. 방어/기사도(반격) 축.

**DB 스펙** (`BuffDB.csv:15-16`):
- 위대한 자(GREATMAN): "한 턴에 한 번 능력을 누를 시 이치 1을 소비하고 3가지 고행 중 하나를 가져옵니다. 3가지 고행이 전부 패에 있다면 능력이 '영웅 상태가 됩니다'로 바뀝니다. (고행 카드는 한 번씩만 가져올 수 있음)"
- 영웅(HERO): "영웅 상태 시 이치의 저주 카드가 전부 영웅 카드로 변하며, 턴 시작 시 소유한 영웅 카드 개수당 그 턴에만 힘의 이치 3 증가·방어 3 획득"

#### 사용 방법 (UI/입력)
1. 화면의 **어빌리티 버튼을 좌클릭** (`AbilityButton.OnMouseDown`, `AbilityButton.cs:37-43`)
2. **대상 지정 없음** — 클릭 즉시 발동 (홍단향과 달리 화살표 없음)
3. 버튼은 전투 진입 시 활성화, 보상 단계에서 비활성화. 게오르크/홍단향만 버튼이 스폰됨 (`PlayerInterfaceServer.cs:41-63`)

#### 발동 조건·제약 (`TargetObject.CharacterSpecific.cs:20-56`)

| 항목 | 값 |
|---|---|
| 코스트 | 이치 **1** |
| 페이즈 제약 | `PLAYER_ACTIVE` 에서만 (서버 검증) |
| 횟수 제한 | **턴당 1회** + **전투당 총 3회** (고행 종류당 1회) |
| 네트워크 | `[Command(requiresAuthority=false)]` + sender 소유권 검증 (스푸핑 방지) |

#### 메커니즘 상세
1. 미획득 고행 중 무작위 1종(`Random.Range(0,3)`)을 패에 생성: **고행 I(G0) / 고행 II(G1) / 고행 III(G2)**
   - 「저주조차 찬란하다(BRILLIANTCURSE)」 버프 보유 시 강화판(`_E`)으로 생성
   - 고행 III 획득 시 `GOHANG3_DEBUFF`(다른 모든 카드 비용 +1), 고행 II 획득 시 `GOHANG2_DEBUFF` 부여
2. **저주 사후효과 "고행길"**: 저주(CURSE) 카드가 턴 종료 시 손에 남으면 발동 —
   G0: 방어 10 삭감 / G1: 붕괴 1 / G2: 현재 주석 처리(무효과)
3. **영웅 변신 조건**: 고행 3종(G0·G1·G2, `_E` 포함)이 **동시에 패에 존재** (`HasAllGohengInHand`). 고행은 재획득 불가이므로 사실상 3장을 전부 아껴 모아야 함.
4. **변신 시** (`GeorkTransform`, `TargetObject.CharacterSpecific.cs:84-98`):
   - `isTransformed = true`, 고행 디버프 제거
   - HERO 버프 부여 — 값 = **잔여 3턴** (`HERO_DURATION_TURN`)
   - **저주 덱 전체(패·뽑을덱·버린덱)가 영웅(HERO) 카드로 전환** (`G0~G7 → G0_H~G7_H`, `GamePlayerDeck.DeckOps.cs:547-590`; 총괄 덱은 미변경)
   - Spine "Transform" 연출(2.667초) 후 영웅 대기 자세
5. **영웅 턴 보너스** (`CardData_Geork.cs:412-443`): 턴 시작 시 (패+뽑을덱+버린덱의 영웅 카드 수) × 3 만큼 **힘의 이치 증가 + 방어 획득**, 턴 종료 시 이치 보너스만 회수.
6. **해제**: 매 턴 종료 HERO -1 → 0이면 원상 복귀(영웅 카드 → 저주 원복).

#### 시너지·연계 카드
- **G61 신의 뜻**: 기본 피해 2, 영웅 상태면 **60** / **G62 구세주**: 영웅 상태면 아군 전체 방어 25
- **GOHANG3 버프**(카드 획득): 모든 카드 비용 **1 고정** / 고통의 메아리: 저주 사용 시 적 전체 3 피해
- 기사도 지원 버프: 추하기에 기사답게(반복 +1), 절대 강자(성공 시 힘의이치 1)
- 압도(APDO) 시스템: 8스택 시 몬스터 행동 1턴 지연(성공마다 요구치 +4), 「자비란 없다」로 스택 소각 피해

### 7-2. 에리스 (ERIS) — 「파괴의 권능」 + 「꿈을 본 인형(광기 변신)」

**컨셉**: **어빌리티 버튼이 없는 유일한 캐릭터 — 고유 능력이 전부 패시브**. 체력을 태워 피해 배수를 얻는 자해 딜러. 이치가 3이 아닌 **2**인 것이 그 대가.

**DB 스펙** (`BuffDB.csv:31, 34`):
- 파괴의 권능: "공격 카드 사용 시 체력이 2씩 감소되지만, 체력이 절반 이하면 공격 카드가 2배 피해를, 광기 상태 시 3배 피해를 줍니다. 이 효과로는 체력 1 밑으로 떨어지지 않습니다."
- 꿈을 본 인형: "체력 1일 시 ??? 상태로 진입… 한 전투에 한 번, 현재 체력의 2배 이하의 공격을 받고 사망 시 체력 1로 살아남습니다."

#### 파괴의 권능 — 자동 발동 메커니즘 (`TargetObject.CharacterSpecific.cs:131-154`)

| 항목 | 값 |
|---|---|
| 트리거 | **공격(ATTACK) 카드 사용 확정 시점** — 카드 실행 파이프라인에서 자동 |
| 코스트 | **체력 2** (방어 무시 순수 소모, HP 1 미만 불가) — 이 소모도 템페스토소(10당 1드로우) 누적 대상 |
| 배수 | 기본 1 / **HP ≤ 최대의 절반 → 2배** / **광기(MAD) → 3배** |
| 추가 배수 | 단말마(DEATHTHROES) 보유 시 +1배 (대신 받는 피해 2배) / E51 「아프나요?」는 최종 ×2 |
| 면제 | E10 「뒤틀리는 생명」은 체력 소모·배수 미적용 |
| 적용 위치 | 몬스터 피해 계산의 **가장 첫 단계**에서 곱해짐 → 붕괴·개화와 곱연산, 월식/별무리 방어 전환량도 함께 증폭 |
| 리셋 | 카드 1회 실행분(도돌이표 2회 반복 포함)까지 유지 후 배수 1로 복귀 |

#### 꿈을 본 인형 — 광기(MAD) 변신
- 상태: `ErisMode { NORMAL, ANGER, MAD }` (ANGER는 치트 전용, 실플레이 도달 불가)
- **발동**: 치사 피해(일반/고정/정적 3경로 모두)를 받을 때 광기 상태가 아니면 → **HP 1로 생존 + 광기 변신** (`TargetObject.Damage.cs:48-53` 외)
- 변신 연출: "Change1" 애니메이션 → MAD 진입 → 전용 대기/카드 애니메이션 프리픽스("V")로 전환
- 광기 상태에서 다시 HP 0 → 실제 사망
- **게임오버 판정 면제**: 전멸 집계에서 에리스는 제외 (`GamePlayer.cs:121`) — §13, §14 참고

⚠ **DB 스펙과 코드 불일치** (기획 확정 필요):

| DB 스펙 | 실제 코드 |
|---|---|
| "한 전투에 한 번" | 횟수 제한 없음 (광기만 아니면 무제한 생존) |
| "현재 체력 2배 이하의 공격" 조건 | 조건 없음 — 어떤 피해든 생존 |
| "체력 2 이상 복귀 시 ??? 해제" | 해제 코드 없음 — 전투 내내 광기 유지 |
| ERIS_NORMAL/2ND/3RD 버프 | enum·CSV만 존재, 부여/참조 코드 0건 |

#### 시너지·연계
- **템페스토소**: 체력 10 잃을 때마다 드로우 — 파괴의권능 소모와 정합
- **창조의 권능**: 방어 획득이 체력 회복 우선으로 전환 / **허물 강화**: 회복→방어 전환
- **월식**: 이번 턴 넣은 피해만큼 방어 (증폭된 피해 기준) / **별무리**: 공격=피해 절반 방어, 전략=패 복귀, 그 외=비용 -2
- **종말의 징조**: 카드가 뽑을덱→버린덱 이동마다 적 전체 2 피해 (덱 밀링 축)
- **이분법**: 턴 시작 체력 2 소모 + 「뒤틀리는 생명」 생성
- **E19 물귀신**: 적 1명과 자신 모두 붕괴 99 (초고위험 콤보) / **E57**: 은하수 카드 수만큼 비용 감소

### 7-3. 홍단향 (HONGDANHYANG) — 「철귀(鐵鬼)」 소환수 조종

**컨셉**: 전투 시작 시 자동 소환되는 상시 소환수 "철귀"를 아군/적 위에 재배치하며 싸우는 전략/서포트형. 카드 풀도 전략 37장으로 최다, 평균 코스트 최고(2.58).

**DB 스펙** (`BuffDB.csv:6`): "철귀는 크기 버프를 올리거나 내릴 수 있으며(최솟값 1) 단향의 버프와 연결되고, 정령 자체에 카드로 효과를 부여할 수 있습니다."

#### 사용 방법 (UI/입력) — 타겟 지정형
1. **어빌리티 버튼 좌클릭** → 베지어 곡선 화살표 발생 (`AbilityButton.cs:39-40`, `AbilityCtrlArrow.cs`)
2. 화살촉을 대상 위로 이동 — 적이면 빨강, 아군이면 파랑으로 화살표 색 변경
3. **좌클릭을 떼면** 그 대상 위로 철귀 이동 명령이 서버 카드 큐에 등록
4. 우클릭 또는 마우스를 화면 하단 밖으로 내리면 취소

#### 메커니즘 상세

**어빌리티 카드 "HA 철귀이동"** (`CardDB.csv:2`, `CardData_DanHyang.cs:70-79`):

| 항목 | 값 |
|---|---|
| 코스트 | **0 이치** |
| 사용 횟수 | **무제한** (턴당 제한 없음) — ⚠ 페이즈 검증도 없음(버튼 가시성만이 게이트) |
| 대상 | 아군·적 TargetObject 모두 |
| 처리 | 일반 카드와 동일한 서버 큐 실행, 단 카드 큐 UI·덱북·경험치 표시에서는 전역 제외 |
| 연출 | 단향 공격 모션 + 대상별 전용 보이스(자신/타 아군/몬스터 분기) + 철귀 텔레포트(0.34s+0.33s) |

**철귀 크기 (IRONDEMON 버프)**:
- 전투 시작 시 **기본 4 + 영구 성장분**(`AdditionalSizeOfIromDemon` SyncVar) 부여 (`M_TurnManager.cs:560-570`)
- 최솟값 1, 턴 경과로 감소하지 않는 스탯형 버프. 카드로 증감 가능.

**철귀 자동 효과 (매 턴 종료 시, `M_TurnManager.cs:273-310`)**:
```
철귀가 아군 위 → 그 대상에게 크기만큼 방어 부여
철귀가 몬스터 위 → 그 몬스터에게 크기만큼 피해
```

**화합(HWAHAP)**: 화합 특성 카드 사용 시 위 철귀 패시브를 **즉시 1회 추가 발동** (`CardData_DanHyang.cs:81-103`).

**철귀 자동 복귀**: 붙어 있던 몬스터/플레이어가 사망하면 주인에게 복귀. 초기 위치는 홍단향 자신.

#### 시너지·연계
- **철귀 이동 공격 카드**: H0 철의 손톱 / H1 철의 이빨(0코스트 피해 10+자신 붕괴 1) / H8 씹어먹기 — 철귀가 날아가 공격 후 복귀
- **크기 빌드 피니셔**: H29 용철 폭발 — 크기를 1로 리셋하고 감소분 ×8 광역 피해
- **크기 성장**: 성장기(턴 시작 +2), 카드로 영구 성장(`AdditionalSizeOfIromDemon`)
- **손톱·이빨 지원 버프**: 철의 분노(강화+화합 부여), 칼날 손질(턴 시작 1장씩 생성), 화가나는구나(이번 턴 2배)
- **제2축 — 꽃가루/개화**: 꽃가루(턴 종료 아군 방어/적 피해 후 1스택 감소, 시전자별 개별 스택), 개화(아군 피해 감소/적 피해 증가, 턴 시작 소멸), H53 독기에 죽어라(꽃가루 3배 피해), 향기로운 꽃(턴 시작 전체 꽃가루 2)
- **이치 조작**: H5/H6(최대 이치 +1), H7/H10(현재 이치 +1), H60 홍씨 가문의 명예(이치 1 + 잡은 보스 수만큼 추가)

### 7-4. 3인 비교 요약

| | 게오르크 「위대한 자」 | 에리스 「파괴의 권능」 | 홍단향 「철귀」 |
|---|---|---|---|
| 유형 | 액티브 (버튼) | **패시브 전용** (버튼 없음) | 액티브 (버튼 + 화살표 타겟팅) |
| 코스트 | 이치 1 | 공격 카드마다 체력 2 | 0 |
| 사용 제한 | 턴 1회 / 전투 3회 | 자동 | 무제한 |
| 대상 지정 | 없음 | 없음 | 아군/적 드래그 지정 |
| 초기 이치 | 3 | **2** | 3 |
| 정체성 | 저주 수집 → 영웅 변신 | 저체력 배수 딜 + 광기 부활 | 소환수 위치 전략 + 꽃가루 |
| 플레이 패턴 | 3턴간 고행을 아껴 모으는 셋업 → 3턴 폭발 | HP 관리하며 배수 구간 유지 | 철귀 배치·크기 성장 → 화합/폭발 |

---

## 8. 버프/디버프 전체 목록

`BuffType` enum 48종 중 43종이 BuffDB.csv 등록 (미등록 5종: NONE, DEFENSE, MOMISPOWERFUL, CARDCOSTONE, SUHOJA — 미구현/내부용).

버프 속성: `value`(스택), `isDebuff`, `isInfinity`(영구), `isDecrease`(턴 시작 -1), `isSeparate`(시전자별 개별 스택 — 꽃가루 등). 훅 4종(턴 시작/드로우 후/카드 사용 시/턴 종료)에 델리게이트 등록 방식.

### 공통 상태 (7)
| 버프 | 효과 |
|---|---|
| 붕괴(BOONGGUI) | 공격 피해 받을 때 ×1.5 (내림) |
| 쇠락(SOIRAK) | 공격 피해 줄 때 25% 감소 |
| 힘의 이치(ICHI_ATTACK) | 카드 피해량 ± 수치 |
| 방어의 이치(ICHI_DEFENSE) | 카드 방어량 ± 수치 |
| 압도(APDO) | 8스택 도달 시 소모하고 몬스터 행동 1턴 지연, 성공마다 요구치 +4 |
| 날개 감싸기(WRAPWINGS) | 이번 턴 방어 2배 |
| 빈틈없는 자세(CLOSEPOSE) | 이번 턴 공격마다 방어 3 |

### 게오르크 계열 (14)
위대한 자 / 영웅 / 고행(비용 1 고정) / 고행2·3 디버프 / 북방의 위대한 투사(턴 시작 힘의이치 1) / 고통의 메아리(저주 사용 시 적 전체 3) / 저주조차 찬란하다(고행 강화판 획득) / 추하기에 기사답게(기사도 +1회) / 절대 강자(기사도 성공 시 힘의이치 1) / 자비란 없다(압도 소각: 1당 피해 3) / 노병의 지혜(위치 변경마다 아군 전체 방어 5) / 영웅의 선율(축복 사용 시 1드로우)

### 에리스 계열 (13)
꿈을 본 인형(기본/진입전/???) / 파괴의 권능 / 창조의 권능(방어→회복 우선) / 별무리(다음 카드 강화) / 템페스토소(체력 10 손실당 1드로우) / 월식(준 피해만큼 방어) / 도돌이표(공격 카드 2회) / 종말의 징조(덱 이동마다 적 전체 2) / 뒤틀림의 끝(만피 시 방어→적 전체 피해 전환) / 허물 강화(회복→방어) / 단말마(권능 +1배·받는 피해 2배) / 이분법(턴 시작 체력 2 소모+토큰 생성)

### 홍단향 계열 (9)
크기(IRONDEMON) / 꽃가루 / 개화 / 철의 분노 / 칼날 손질 / 화가나는구나 / 성장기 / 꽃과 철의 분노(3장 사용 시 화합) / 향기로운 꽃(턴 시작 전체 꽃가루 2)

---

## 9. 몬스터·적 구성

### 9-1. 데이터 구조 (MonsterDB.csv)

- 위치 기반 파싱: 몬스터당 여러 행동 패턴, `Frequency`(합 100 기준 가중치 룰렛) + 패턴 내 `(행동, 수치, 대상)` 순차 시퀀스.
- 대상 지정: FRONT/MIDDLE/BACK/RANDOM/WHOLE 등 — **진영(전·중·후열)** 기반.
- 등급(일반/엘리트/보스)은 CSV가 아니라 몬스터 클래스 코드에서 결정.

### 9-2. 몬스터 22엔트리 (스테이지 1)

**일반 10종**: 도끼병사(HP 40, 두번찍기 8) / 방패병사(HP 70, 팀방어) / 창병사(HP 40) / WacherA(HP 15, 순수 디버퍼) / WacherB(HP 15, 순수 버퍼) / Devourer(HP 50, 흡혈·붕괴) / GiantSoldier(HP 100, 단일 30) / SpearManA(HP 30, 예고 후 강공 20×2 시퀀스형) / SpearManB(HP 30) / Guardian(HP 50)

**엘리트 4종**: Happy·Saddy(HP 200) / E3·E4(HP 200) — ⚠ Saddy는 frequency 합 150(버그)

**보스 3종** (HP 9999, 그리스 신화 작명, 랜덤 1종 등장):
- **모모스(Momos)** — 3턴 로테이션, 캐릭터별 조우 대사, 사망 시 '달의 소녀' 나레이션
- **아파테스(Apates)** — 모모스 동형 패턴, 전용 BGM
- **게라스(Geras)** — 1페이즈부터 격노(Enrage)

**NPC 4종**: 메르쿠리우스(카드 상점) / 섀도우맨(아이템 상점) / 류진솔·소피아(캠프)

### 9-3. 몬스터 그룹 (hazard 밴드, 15그룹)

| 위험도 | 그룹 | 구성 경향 |
|---|---|---|
| 0–10 | 기본 병사조 | 표준 3인 / 도끼×3 |
| 5–15 | 병사+Wacher | 병사 2 + 버퍼/디버퍼 |
| 10–20 | 강화조 | 병사 + Devourer |
| 15–25 | 위험조 | Devourer×3 → GiantSoldier×3 / **Twins(Happy+Saddy)** |

전투당 몬스터는 최대 3마리(슬롯 3~5).
⚠ 스테이지 2·3 몬스터는 프리팹/스크립트(S2 12종, S3 11종+엘리트 2종)만 존재하고 **MonsterDB 미등록 → 스폰 불가** (데이터 추가 대기 상태).

---

## 10. 3인 협동 규칙

### 10-1. 진영(playerOrder) — "순서"가 아니라 "위치"

- `playerOrder[0]=후열, [1]=중열, [2]=전열` — 몬스터의 타겟 지정(FRONT 등)이 이 열을 참조.
- **턴은 번갈아 진행하지 않는다.** `PLAYER_ACTIVE` 동안 3인이 동시에 카드 사용, 실행 순서는 서버 큐 도착순(FIFO).
- 진영 스왑: 맵에서 수락/거절 방식, 전투 중에는 전진/후진 카드로. 「노병의 지혜」 보유자는 스왑마다 아군 전체 방어 5.

### 10-2. 아군 지원 수단

| 수단 | 내용 |
|---|---|
| MEMBER/TEAM 카드 | 아군 1인(자신 제외)/아군 전체 대상 방어·버프 (기본 199장 중 MEMBER 12, TEAM 11) |
| 구원(GOOWON) 특성 | 타인 대상 시 수치 **2배** |
| 개화(FLOWER) 전역 합산 | 한 명이 건 개화가 파티 전체 피해 계산에 반영 |
| 골드 양도 | 캠프에서 대상·금액 지정 (서버 검증 포함) |
| 체력 회복 | 캠프 회복권으로 **타인 치료 가능** (소모 주체는 클릭한 플레이어) |
| 손패 엿보기 | 파티원의 손패·덱·이치를 실시간 열람 (잠금 토글 지원) |

### 10-3. 전원 합의 지점

턴 종료(전원 또는 HP 0) / 보상 수령 완료(전원) / 맵 이동(투표+전원 Ready) / 손패 정리 완료(전원).

---

## 11. 보상·상점·휴식·아이템

| 시스템 | 내용 | 상태 |
|---|---|---|
| 전투 보상 | 인당 카드 3택1 + 골드 10, 스킵 가능, 전원 완료 시 맵 복귀. 이탈자 보상은 호스트가 탭으로 인계 | 구현 (보상 테이블은 TODO 하드코딩) |
| 카드 상점 | 인당 6장 롤, 1골드(임시), 구매 시 품절 처리. 강화·제거 겸업 | 구현 (⚠ 강화/제거 무료) |
| 캠프(휴식) | 진입 시 인당 회복권 1 (회복량 15), 골드 양도 | 구현 |
| 이벤트 방 | 긍/부정 모두 연출만 | **미구현** |
| 아이템(유물/유산) | `ItemType{ARTIFACT, LEGACY}`, 등급 4종, 발동 트리거 12종 프레임워크 존재 | **스텁** — DB에 TEST 2행뿐, 효과 메서드 빈 함수, 획득 경로 0건. `GamePlayerItem`이 이름/번호 키 불일치로 획득 시 예외 발생 구조 |

---

## 12. 저장·메타 진행

**저장 대상** (런 스냅샷, `SaveData.cs` / `M_SaveManager.cs`): 플레이어(SteamID·캐릭터·HP·**전체 덱**), 맵 전체(방 타입·좌표·활성), 거점.
**미저장**: 골드, 행동비용, 맵 보스 상태, 아이템, 버프.

- 저장 트리거: **테스트 버튼뿐** (자동 저장 없음). 로드: 로비명 `"load"` 히든 트리거.
- 저장 방식: `persistentDataPath/save.dat` + `BinaryFormatter` (⚠ deprecated API, 보안·호환성 취약).
- **메타 프로그레션(런 간 영구 성장·해금·통계)은 전무.** "유산(LEGACY)"은 이름만 존재.

---

## 13. 승리·패배 조건

| 판정 | 조건 | 처리 |
|---|---|---|
| 전투 승리 | 몬스터 리스트 0 | BATTLE_END → 보상 → 덱/이치 리셋 → 맵 복귀 |
| 플레이어 사망 | HP 0 (에리스는 광기 변신으로 1회성 아님 생존) | TargetObject 완전 제거. **부활 없음**. 턴 종료 대기에서 제외 |
| 게임 오버 | **전원 HP 0** (1인 생존 시 계속) | 게임오버 팝업 → 메뉴 복귀 + 로비 탈퇴 |
| 게임 클리어 | ⚠ **미구현** — 맵 보스를 잡아도 일반 전투처럼 보상 후 계속 | — |

⚠ 게임오버 집계에서 에리스가 영구 제외(`!IsEris`)되어 있어 **에리스가 파티에 있으면 전멸해도 게임오버가 발생하지 않는 구조적 문제** 있음 (`GamePlayer.cs:117-125`).

---

## 14. 미구현·기획 확인 필요 목록

### A. 시스템 공백 (컨텐츠 작업 후보)
1. **게임 클리어/엔딩 없음** — 보스 처치 후 런 종료 처리 부재
2. **스테이지 1→2→3 진행 없음** — enum·아이콘·S2/S3 몬스터 스크립트만 존재, DB 미등록
3. **난이도 선택 없음** — `GameLevel{EASY,NORMAL,HARD}` 참조 0건
4. **아이템(유물)/유산 시스템 스텁** — DB TEST 2행, 효과 빈 함수, 획득 경로 없음 + 키 불일치 예외 구조
5. **메타 프로그레션 전무** — 세이브는 개발용 런 스냅샷만
6. **이벤트 방 실효과 없음** (연출만)
7. **보상 테이블 하드코딩** (골드 10/카드 3, TODO 주석)
8. **거점 등급 효과 없음**
9. **엘리트 전용 몬스터 테이블 없음** (일반과 동일 스폰)
10. **시작 덱 구성 코드 전체 주석 처리** — 신규 시작 시 빈 덱

### B. 밸런스·정합성 이슈
11. 전투 종료 시 이치 **3 하드코딩 리셋** — 에리스(2)·최대이치 성장분 무시 (`RewardService.cs:53-54`)
12. `ICHI_LIMIT(6)` 상한 미적용 (`limitiChi` 참조 0건)
13. 게임오버 판정 에리스 영구 제외 → 에리스 포함 파티 전멸 불가
14. 에리스 「꿈을 본 인형」 DB 스펙 3항목(전투당 1회/2배 조건/해제) 미구현
15. 손패 10장 상한 미구현 (레이아웃 상수만 존재)
16. 홍단향 철귀이동 **페이즈/횟수 제약 전무** (몬스터 턴 중에도 이동 가능성)
17. Saddy 행동 frequency 합 150 (100 초과)
18. Guardian·Saddy의 상시 버프 행 파서 주석 처리로 미작동
19. 카드 강화/제거 골드 미소모, 상점 가격 1골드 임시값
20. 행동비용 초기값 3 vs 최대 30 — 의도 확인 필요

### C. 코드 품질·데이터 오류
21. `BinaryFormatter` 세이브 (deprecated·취약)
22. 로비 비밀번호 미검증 / 캐릭터 중복 선택 방지 없음
23. CSV 오타: `BLESS `(끝 공백) 10건, `BYULMOORI `(enum 끝 공백 → 특성 툴팁 로드 실패), `Garde`(=Grade)
24. Description.csv 툴팁 누락 11종 (`@크기`, `@고행길`, `@이치의저주`, `@파괴의권능` 등 핵심 용어 포함) + 조사 붙은 토큰 조회 실패 (공백 split 구조 결함)
25. BuffDB 설명 미작성 5건 (GOHANG2/3_DEBUFF, GOHANG3, ERIS_2ND/3RD)
26. 에리스에게도 HA 어빌리티 카드가 스폰됨 (사용 경로 없는 죽은 코드)
27. 홍단향 어빌리티 카드가 `destroyCardList`에 무한 누적 (파괴되지 않음)
28. `sizeOfIronDemon` 프로퍼티 null 체크 없음 (버프 제거 상태에서 NRE 위험)
29. G2 저주 사후효과 주석 처리로 무효과
30. `CardType.WOUND` / `BattleTurn.PLAYER_ORDERSELECT` 데드 enum
31. CARD_AUDIT.md의 G61 지적은 현재 코드에서 이미 해결됨 (감사 문서가 낡음)
